name: "shared_config"
version: 1.0.4
description: Package to shared theme style for entire app
repository: https://github.com/harrytran998/53-days-flutter-challanges/tree/main/shared

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.9.2

flutter:
  uses-material-design: true
